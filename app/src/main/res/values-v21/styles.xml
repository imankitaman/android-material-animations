<?xml version="1.0" encoding="utf-8"?>
<resources>

    <style name="MyAppTheme" parent="@style/Theme.AppCompat.Light.NoActionBar">
        <item name="android:windowTranslucentStatus">true</item>
        <item name="android:windowEnableSplitTouch">false</item>
        <item name="android:colorControlHighlight">@color/ripple_color</item> 
        <item name="android:windowSharedElementsUseOverlay">false</item>
        <item name="android:windowDrawsSystemBarBackgrounds">true</item>
        <item name="android:statusBarColor">@color/colorPrimaryDark</item>

        <!-- specify enter and exit transitions -->
        <!-- explode suggested by google docs but window enter transition one is better -->
        <!--<item name="android:windowEnterTransition">@transition/explode</item>-->
        <!--<item name="android:windowEnterTransition">@transition/window_enter_transition_one</item>-->


        <!-- enable window content transitions -->
        <item name="android:windowActivityTransitions">true</item>

        <!-- specify shared element transitions -->
        <!--<item name="android:windowSharedElementEnterTransition">@transition/session_details_shared_enter</item>-->
        <!--<item name="android:windowSharedElementExitTransition">@transition/session_details_shared_return</item>-->

        <!--specify shared element transitions -->
        <!-- Demo by android doc  -->
        <!--<item name="android:windowSharedElementEnterTransition">@transition/change_image_transform</item>-->
        <!--<item name="android:windowSharedElementExitTransition">@transition/change_image_transform</item>-->
    </style>


    <style name="MyAppTheme.ToolBarStyle" parent="ThemeOverlay.AppCompat.Dark.ActionBar">
        <item name="android:background">@color/colorPrimary</item>
        <item name="android:textColorPrimary">@android:color/white</item>
        <item name="android:textColor">#ffffff</item>
        <item name="android:textColorPrimaryInverse">#ffffff</item>
        <item name="android:windowBackground">@color/colorPrimary</item>
    </style>

</resources>